{
    "name": "takeinitiative",
    "version": "1.0.14",
    "private": true,
    "type": "module",
    "scripts": {
        "___ SETUP ___": "",
        "setup_environment": "(pnpm i) && (docker compose -p takeinitiative -f compose.dev.yml up -d postgres)",
        "___ DEV ___": "",
        "dev": "pnpm run setup_environment && (pnpm turbo run dev --ui tui)",
        "api": "pnpm run setup_environment && (pnpm turbo watch dev --filter=@ti/api)",
        "web": "pnpm run setup_environment && (pnpm turbo watch dev --filter=@ti/web)",
        "___ UTILITY ___": "",
        "build": "pnpm turbo run build",
        "test": "pnpm turbo run test"
    },
    "devDependencies": {
        "turbo": "2.9.16"
    },
    "packageManager": "pnpm@10.33.0",
    "volta": {
        "node": "24.15.0"
    }
}
